import { navigage } from '../navigation'

import createDataContext from './createDataContext'
import api from '../api/api'

const userReducer = (state, action) => {
    switch (action.type) {
        default: 
            return state
    }
}

